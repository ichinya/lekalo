{
  "name": "lekalo-target-node-typescript",
  "private": true,
  "description": "Build-time only dependency custody for the vendored single-file adapter artifact. The committed adapter.mjs embeds the compiler bundle; neither scan-time nor fixture execution reads this directory, node_modules, or any package manager.",
  "type": "module",
  "scripts": {
    "preinstall": "node -e \"process.stderr.write('poison: package scripts must never run')\" && exit 1",
    "postinstall": "node -e \"process.stderr.write('poison: package scripts must never run')\" && exit 1",
    "prepare": "node -e \"process.stderr.write('poison: package scripts must never run')\" && exit 1",
    "test": "node -e \"process.stderr.write('poison: package scripts must never run')\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "typescript": "5.9.3",
    "zod": "^3.25.76"
  },
  "engines": {
    "node": ">=18"
  }
}
