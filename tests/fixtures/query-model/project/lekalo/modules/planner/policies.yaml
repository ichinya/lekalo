schema_version: "0.2.16"
definitions:
  - id: planner.task_read
    kind: policy
    version: 1
    description: Task reads stay inside the actor tenant
    applies_to:
      - "planner.focus_task"
    decision: allow
