schema_version: "1.0.0"
definitions:
  - id: planner.task
    kind: entity
    version: 1
    optimizer_hint: precompute
    fields:
      - name: task_id
        type: "planner.task_id"
        required: true
    identity:
      - task_id
