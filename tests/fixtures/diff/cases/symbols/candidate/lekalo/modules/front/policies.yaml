schema_version: "1.0.0"
definitions:
  - id: shop.deny_delete
    kind: policy
    version: 1
    description: "Deleting items is out of scope"
    applies_to:
      - "shop.create_item"
    decision: allow
