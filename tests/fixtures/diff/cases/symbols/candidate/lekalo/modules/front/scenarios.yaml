schema_version: "1.0.0"
definitions:
  - id: shop.create_flow
    kind: scenario
    version: 2
    description: "Create flow"
    summary: "Creating an item emits the created event."
    covers:
      - "shop.create_item"
