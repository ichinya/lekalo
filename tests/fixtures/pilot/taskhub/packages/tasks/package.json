{
  "name": "@taskhub/tasks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "node --test",
    "gate:build": "node gates/verify.mjs --mode build"
  }
}
