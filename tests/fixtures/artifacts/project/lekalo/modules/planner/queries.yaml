schema_version: "0.2.16"
definitions:
  - id: planner.count_focused
    kind: query
    version: 1
    description: "Count focused tasks"
    reads:
      - "planner.task"
    returns:
      ref: "planner.task_state"
